# 환경 설정 파일 (setup.sh 에 의해 생성, 민감정보 포함)
env.conf

# 환경변수 적용된 렌더링 결과물 (setup.sh 에 의해 생성)
rendered/

# VM 이미지 파일 (용량이 크므로 git 관리 제외)
*.qcow2
vm-images/

# 스크립트 실행 시 생성되는 임시 yaml (poc-setup/ 하위에도 생성됨)
poc-setup/
datasource-*.yaml
template-*.yaml
nmstate-cr.yaml
nncp-*.yaml
nad-*.yaml
netpol-*.yaml
resourcequota-*.yaml
kubedescheduler.yaml
vmim-*.yaml
poc-*.yaml

# macOS
.DS_Store
