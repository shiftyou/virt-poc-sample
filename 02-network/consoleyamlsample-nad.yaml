apiVersion: console.openshift.io/v1
kind: ConsoleYAMLSample
metadata:
  name: poc-bridge-nad
spec:
  title: "POC Linux Bridge NAD 등록"
  description: "NNCP로 생성된 Linux Bridge를 VM 보조 네트워크로 등록합니다. NNCP(poc-bridge-nncp)가 Available 상태인 후 적용하세요. 등록 후 VM 생성 시 'Network interfaces'에서 선택할 수 있습니다."
  targetResource:
    apiVersion: k8s.cni.cncf.io/v1
    kind: NetworkAttachmentDefinition
  yaml: |
    # =============================================================================
    # [Console YAML Sample] Linux Bridge NetworkAttachmentDefinition 등록
    # OpenShift Console > + 버튼 (Import YAML) 에서 붙여넣기 후 사용
    #
    # 적용 전 아래 값을 환경에 맞게 수정하세요.
    #   - namespace             : VM이 실행될 네임스페이스 (예: poc-vm-management)
    #   - resourceName의 br1    : NNCP에서 생성한 Bridge 이름 (env.conf BRIDGE_NAME)
    #   - config의 bridge: br1  : 동일하게 Bridge 이름으로 변경
    # =============================================================================
    apiVersion: k8s.cni.cncf.io/v1
    kind: NetworkAttachmentDefinition
    metadata:
      name: poc-bridge-nad
      namespace: poc-vm-management
      annotations:
        k8s.v1.cni.cncf.io/resourceName: bridge.network.kubevirt.io/br1
    spec:
      config: '{"cniVersion":"0.3.1","name":"poc-bridge-nad","type":"cnv-bridge","bridge":"br1","macspoofchk":true,"ipam":{}}'
