apiVersion: console.openshift.io/v1
kind: ConsoleYAMLSample
metadata:
  name: poc-bridge-nncp
spec:
  title: "POC Linux Bridge NNCP 생성"
  description: "워커 노드에 Linux Bridge를 생성합니다. 물리 NIC(BRIDGE_INTERFACE)을 Bridge 포트로 연결하여 VM이 물리 네트워크에 직접 연결될 수 있도록 합니다. 적용 전 노드의 실제 NIC 이름을 확인하세요."
  targetResource:
    apiVersion: nmstate.io/v1
    kind: NodeNetworkConfigurationPolicy
  yaml: |
    # =============================================================================
    # [Console YAML Sample] Linux Bridge NNCP 생성
    # OpenShift Console > + 버튼 (Import YAML) 에서 붙여넣기 후 사용
    #
    # 적용 전 아래 값을 환경에 맞게 수정하세요.
    #   - interfaces[0].name       : 생성할 Bridge 이름 (env.conf BRIDGE_NAME, 예: br1)
    #   - interfaces[0].bridge.port: 물리 NIC 이름    (env.conf BRIDGE_INTERFACE, 예: ens4)
    # =============================================================================
    apiVersion: nmstate.io/v1
    kind: NodeNetworkConfigurationPolicy
    metadata:
      name: poc-bridge-nncp
    spec:
      nodeSelector:
        node-role.kubernetes.io/worker: ""
      desiredState:
        interfaces:
          - name: br1
            description: "POC VM용 Linux Bridge (ens4 기반)"
            type: linux-bridge
            state: up
            ipv4:
              enabled: false
            ipv6:
              enabled: false
            bridge:
              options:
                stp:
                  enabled: false
              port:
                - name: ens4
